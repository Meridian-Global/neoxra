anthropic>=0.39.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
fastapi>=0.111.0
python-multipart>=0.0.9
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg[binary]>=3.2.3
# Server-side rendering (Pillow-based, no browser install needed).
# Install separately from a published package or VCS URL — do not use a local path here
# as it breaks installs in CI/Docker and for users without a sibling checkout.
# Integration dependencies (Gmail, LinkedIn): see requirements-integrations.txt
